= render "shared/search"
- if @records.any?
  %h3 Results
  
  %p
    = pluralize(@records.total_entries, "record")
    matched the search query:
    %em= "'" + params[:query] + "'"
  
  = render "shared/records"

  %p
    Show results in 
    = link_to "JSON", params.merge(:format => :json)
    or
    = link_to "MARCXML", params.merge(:format => :marcxml)
    format.
- else
  %p{:style => "padding-top: 20px"} 
    No records found.